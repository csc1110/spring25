package wk12.sectionb;

public interface Shape {

    /**
     * some
     * @return something
     */
    double area();

    /**
     * returns true if shapes are the same
     * @param s other shape to check
     * @return true if the have the same area
     */
    boolean isSame(Shape s);

}
